
<!-- Dashboard -->
<div id="dashboard">

    <!-- Navigation
    ================================================== -->

    <!-- Responsive Navigation Trigger -->
    <a href="#" class="dashboard-responsive-nav-trigger"><i class="fa fa-reorder"></i> Dashboard Navigation</a>

    <div class="dashboard-nav">
        <div class="dashboard-nav-inner">

            <ul data-submenu-title="Main">
                <li><a href="/dashboard"><i class="sl sl-icon-settings"></i> Dashboard</a></li>
            </ul>

            <ul data-submenu-title="Listings">
                <li class="active"><a href="/listings/new"><i class="sl sl-icon-plus"></i> Add Listing</a></li>
<!--                      <li><a href="/listings/new"><i class="sl sl-icon-plus"></i> Add Listing</a></li>
 -->            </ul>
<!--
            <ul data-submenu-title="Account">
                <li><a href="dashboard-my-profile.html"><i class="sl sl-icon-user"></i> My Profile</a></li>

                <li><a href="/users/sign_out" method="DELT"><i class="sl sl-icon-power"></i> Logout</a></li>
            </ul>
 -->
        </div>
    </div>
    <!-- Navigation / End -->


    <!-- Content
    ================================================== -->
    <div class="dashboard-content">

        <!-- Titlebar -->
        <div id="titlebar">
            <div class="row">
                <div class="col-md-12">
                    <h2>Add Listing</h2>
                    <!-- Breadcrumbs -->
                    <nav id="breadcrumbs">
                        <ul>
                            <li><a href="#">Home</a></li>
                            <li><a href="#">Dashboard</a></li>
                            <li>Add Listing</li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
            <%= simple_form_for(@listing) do |f| %>
                <%= render partial: 'form', locals: {f: f} %>
            <%end%>
            </div>
            <!-- Copyrights -->
            <div class="col-md-12">
                <div class="copyrights">© 2017 LocalFitt. All Rights Reserved.</div>
            </div>

        </div>

    </div>
    <!-- Content / End -->


</div>
<!-- Dashboard / End -->


</div>
<!-- Wrapper / End -->


<!-- Scripts
================================================== -->




<% content_for(:after_js) do %>
  <%= javascript_tag do %>

    var map;
    var marker;

// geolocation with HTML5 through browser
    function getLocation() {
     if (navigator.geolocation) {
        navigator.geolocation.getCurrentPosition(function(p){
        console.log(p.coords.longitude);
          var mapOptions = {
          center: new google.maps.LatLng({lng: p.coords.longitude,lat: p.coords.latitude}),
          zoom: 14
          }
          initMap(mapOptions);
          });
    } else {
      var mapOptions = {
        //  document.getElementById('lat').value
         // document.getElementById('lng').value
        center: new google.maps.LatLng(-25.363, 131.044),
        zoom: 14
        }
      initMap(mapOptions);
       }
   }
      function initMap(mapOptions) {
        var handler = Gmaps.build('Google');

        handler.buildMap({ provider: mapOptions, internal: { id: 'map_set_location' } }, function() {
          map = handler.getMap();
          map.addListener('click', function(e) {
            addMarker(e.latLng);
          });
        });
      }
      function addMarker(latLng){
        if (marker) marker.setMap(null);
        marker = new google.maps.Marker({
          position: latLng,
          map: map
        });
        $('#listing_latitude').val(latLng.lat());
        $('#listing_longitude').val(latLng.lng());
      }
  <% end %>
<% end %>



